# Memory Stick protocol decoder

Requires the typedsigrokdecode module.

Currently sort of works, but mode auto switching does not work, and the state machine is still very messy and requires a redesign.
